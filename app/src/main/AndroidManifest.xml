<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.website.consulta">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.ConsultaItemsVenta"></activity>
        <activity android:name=".View.Modificarve" />
        <activity android:name=".View.Anularve" />
        <activity android:name=".View.Consultarve" />
        <activity android:name=".View.Nventas" />
        <activity android:name=".View.Emision" />
        <activity android:name=".View.ConsultaActivity" />
        <activity android:name=".View.MenuActivity" />
        <activity android:name=".View.Principal" />
        <activity android:name=".View.Inicio" />
        <activity android:name=".View.splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Motores" />
    </application>

</manifest>